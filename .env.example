# ExchAInge Docker Deployment Environment Variables
# Copy this file to .env and fill in your actual values.

# Supabase Database Configuration
# Get these from your Supabase project settings at https://supabase.com/dashboard.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Application URLs
# For local development use http://localhost.
# For production use https://exchainge.net or your domain.
NEXT_PUBLIC_APP_URL=http://localhost
NEXT_PUBLIC_API_BASE_URL=http://localhost/api

# Privy Authentication
# Get credentials from https://console.privy.io.
NEXT_PUBLIC_PRIVY_APP_ID=your_privy_app_id_here
NEXT_PUBLIC_PRIVY_CLIENT_ID=your_privy_client_id_here
PRIVY_APP_SECRET=your_privy_app_secret_here

# Redis Cache
# Get credentials from https://console.upstash.com.
UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token_here

# Cloudflare R2 Object Storage
# Get credentials from https://dash.cloudflare.com.
R2_ACCESS_KEY_ID=your_r2_access_key_id_here
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key_here
R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
R2_BUCKET_NAME=datasets

# Solana Blockchain Configuration
# For testing use https://api.devnet.solana.com.
# For production use https://api.mainnet-beta.solana.com.
# You can also use RPC providers like Helius or QuickNode for better performance.
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_WALLET_PRIVATE_KEY=your_solana_wallet_private_key_base58_here
SOLANA_DATASET_PRICE_SOL=0.1

# Sui Blockchain Configuration
# Deploy the Move contract first using: cd contracts/move && ./deploy.sh
# Then copy the package ID here.
NEXT_PUBLIC_SUI_PACKAGE_ID=0x_your_deployed_package_id_here
NEXT_PUBLIC_SUI_NETWORK=testnet

# Walrus Decentralized Storage
# Generate a Sui keypair for signing Walrus uploads: sui keygen
# Then export the private key and base64 encode it
WALRUS_SIGNER_KEY=your_base64_encoded_private_key_here
WALRUS_NETWORK=testnet
WALRUS_EPOCHS=5

# Optional features for X402 branch only.
# Not needed for main branch.
# AGENT_WALLET_PRIVATE_KEY=your_agent_wallet_private_key_here
# AGENT_ACCESS_TOKEN=your_agent_privy_access_token_here
# AGENT_MAX_PRICE=1600

# Quick start guide.
# 1. Copy this file with cp .env.example .env
# 2. Fill in your credentials in .env
# 3. Run docker compose up -d
# 4. Access at http://localhost

# Production deployment notes.
# Point your domain DNS to your server IP.
# Update NEXT_PUBLIC_APP_URL to your domain like https://exchainge.net.
# Update NEXT_PUBLIC_API_BASE_URL to your domain plus /api like https://exchainge.net/api.
# Set up SSL certificates.
# Change Solana RPC to mainnet if using mainnet.

# Security reminders.
# Never commit .env file to git.
# Keep private keys secure and backed up.
# Use strong random values for production secrets.
# Rotate credentials regularly.
